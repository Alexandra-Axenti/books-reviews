
import { booksRating } from 'books-reviews/helpers/books-rating';
import { module, test } from 'qunit';

module('Unit | Helper | books rating');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = booksRating([42]);
  assert.ok(result);
});

